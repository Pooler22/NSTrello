<div class="controller">
  <h2>Tablice</h2>
  <table class="table">
    <tr>
      <th>Nazwa</th>
    </tr>
    <% _.each(boards, function (board) { %>
    <tr data-id="<%= board.id%>" data-model="board">
      <td><%= board.id %></td>
      <td><%= board.name %></td>
      <td>Show</td>
      <td>Edit</td>
      <td>Destroy</td>
    </tr>
    <% }) %>
  </table>
</div>
